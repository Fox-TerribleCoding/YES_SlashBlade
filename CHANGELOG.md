# 更新日志

本项目的版本号遵循「已发布内容不可变」的原则：**已上传的文件不覆盖，只发新版本**，
旧版本**归档而不删除**（删除会让锁定 file ID + 哈希的整合包直接坏掉）。

---

## [1.0.16] — 2026-09-17

> **打磨：女仆拔刀剑的"出鞘 / 收鞘"不再生硬跳变。**（属**有意添加的增强**，非还原上游）

### 新增（打磨）

- **女仆出鞘/收鞘两端各留一小段缓动**（默认 1 刻 = 50 ms）

  **背景**：TLM 给女仆的"拔刀剑动作"本身就只有 5 刻的**恒速自转**
  （`rotY(60 + i*48)`，48°/刻），而且做成**二值**的 ——
  窗口内整套出鞘变换加满、窗口一过**整段消失** ⇒ 进出各"啪"地跳一下。
  （对比玩家那条：`LayerMainBlade` 播的是 MMD 成套动作、每帧插值，所以看起来流畅。）

  **本项做的是"把两头磨圆"**：在窗口两端按 `elapsed` 计算权重，
  从"收在鞘里"的姿态**球面插值**到出鞘姿态，位移同步缩放。

  - **窗口中段权重恒为 1，与原实现逐位相同** —— 只改两头，不改变上游那段转动本身；
  - 新配置 `maidBladeEaseTicks`（**热重载**，默认 `1.0` 刻；**设 `0` 即完全回到 TLM 原样**）。

  > ⚠️ **这是本模组有意添加的打磨，不是还原上游**（TLM 没有这个缓动）。
  > 若你要严格对齐参照版本，设 `maidBladeEaseTicks=0`。

  > **说明**：这一项只是**抹掉跳变**。刀本身的动作仍然只是上游那段 5 刻恒速自转 ——
  > 要让女仆像玩家那样有"成套剑技动作"，需要把 MMD 动作搬过去或接连招状态机，
  > 那是另一件事（也超出"把丢掉的还回去"的定位），本轮**没有做**。

---

## [1.0.15] — 2026-09-17

> **修复：开光影时第一人称的刀「水平方向被锁定、竖直方向随视角反着动」。**

### 修复

- **开光影时，第一人称的刀不再跟着视角左右转，竖直方向还随视角反着动**

  **现象**：开启光影后，第一人称的刀**水平方向被锁死**（视角左右转，刀不动），
  而**竖直方向会随视角转动而移动、方向还是相反的** —— 正常来说水平方向应当跟视角、
  竖直方向不该动。

  **根因**：拔刀剑 2.0.7 自带一段「Iris 近似补偿」——
  它检测到 Iris 且正在用光影时会额外施加两次旋转，用来**近似**抵消 Iris 塞进
  `ModelViewMat` 的那层矩阵：

  ```java
  if (isIrisLoaded && Iris.isPackInUseQuick()) {
      poseStack.mulPose(XP, +player.getXRot());   // 与后面那句 -clamp(xRot) 配对
      poseStack.mulPose(YP, yaw + 180.0f);        // 与下面那句 (180 - yaw) 抵消成 360°
  }
  poseStack.mulPose(YP, 180.0f - yaw);
  ```

  而本模组自 1.0.12 起**已经精确处理了 `ModelViewMat`**（姿态基准 =
  `ModelViewMat⁻¹ × 相机旋转`），于是这段近似不再是补偿，而是**纯粹的错误**：

  - `R_y(yaw+180) · R_y(180−yaw) = R_y(360) = 单位阵` ⇒ **偏航被整个抵消**（水平方向锁死）；
  - `R_x(+xRot)` 与 `R_x(−clamp(xRot))` 并不抵消 ⇒ **多出一层俯仰**（竖直方向随视角反着动）。

  **修法**：把这个判断的**来源**改掉 —— 在 `BladeFirstPersonRender` 的构造器里，
  把 `ModList.isLoaded("iris")` 的返回值对本类**隐瞒为 false**（仅此一处、仅此一个 modid），
  于是 `isIrisLoaded` 恒为 false，整段特判自然永不执行。

  这样做的好处：

  - 靶点是 **NeoForge 自己的 `ModList`**（必定可加载、校验必过），
    不像 1.0.10 那次去拦 `Iris.isPackInUseQuick()` —— 那条在 Mixin **校验阶段**就失败，
    还连累了整份混入类作废；
  - 与"那段分支长什么样"**无关** —— 字段一旦为 false，代码怎么改都无所谓；
  - 只影响这一个类的这一个字段，**不碰 Iris、不碰光影包、不碰任何第三方文件**。

- 新增开关 `firstPersonIrisHack`（`auto` 默认 / `keep` / `ignore`）。
  `auto` 只在**本模组确实接管了姿态基准**时才停用那段近似；
  把 `firstPersonPoseBase` 设成 `identity`（回到拔刀剑原行为）时它会被保留。

  > ⚠️ **这一项需要重启游戏才生效** —— 拔刀剑在首次构造时读一次该字段（单例），
  > 与本模组其它"存盘 2 秒生效"的开关不同。

### 诊断

- 新增一行（只在真的停用时打一次）：
  `已对拔刀剑隐瞒「iris 已加载」，从而停用它的 Iris 近似补偿…`

---

## [1.0.14] — 2026-09-17

> 🔴 **严重崩溃修复，建议所有用户升级。**
> **1.0.5 ~ 1.0.13 在"没装车万女仆"的整合包里，任何一次左键挥击都会崩游戏**（例如 ATM10）。

### 修复（严重）

- **没装车万女仆时，任何实体挥击都会崩溃（`NoClassDefFoundError: …EntityMaid`）**

  **现象**：在**没有装车万女仆**的整合包里，玩家（或任何生物）一左键挥击就崩，
  崩溃报告指向 `LivingEntity.handler$…$yes_sb$maidSwing`。

  **根因**：女仆挥刀钩子打在**原版** `LivingEntity#swing` 上（每个实体挥刀都会经过），
  而它调用的桥接类里直接写了 `entity instanceof EntityMaid` ——
  **`instanceof` 一个类就意味着要求 JVM 解析并加载它**。车万女仆缺席 ⇒ 解析失败。

  **为什么 `catch (Throwable)` 兜不住**：失败发生在该桥接类**被链接/校验的阶段**，
  也就是**进入方法体之前**，不在 `try` 的范围内。
  （证据：崩溃栈里只有注入点那一帧、**没有桥接层的帧**。）

  **修法**：新增 `ModPresence` —— 一个**完全不引用任何第三方类型**的判定层：
  - "装没装"只查 `ModList`；
  - "是不是女仆"改为**按类名爬父类链**，不再用 `instanceof`；
  - 桥接类里删掉 `EntityMaid` 的 import，`try` 之前先做这两道判定。

  同时**堵掉同一类隐患**：`SlashBladeBridge`（24 处拔刀剑类型引用）此前会被
  "每次实体渲染"这条路径在拔刀剑缺席时加载 ⇒ 现在同样先查 `ModList` 再进。

### 新增（构建期守卫）

- **`build.ps1` 增加"可选依赖泄漏守卫"**：编译后扫描所有 `.class` 的字节，
  查找拔刀剑 / 车万女仆 / YSM 的类型描述符；与一份显式白名单比对，
  出现白名单外的引用就**让构建直接失败**并打印违规类名。

  这类错误的共性是"写的时候完全看不出问题"（有依赖、能编译、能跑），
  所以必须挡在构建阶段 —— 与当年那道 BOM 守卫同一思路。

### 说明

- 本模组对"依赖可选"的承诺（**缺失时功能自动失效，不崩溃**）在 1.0.5~1.0.13 上并未成立，
  本版已修正；同时用构建期守卫保证同类问题不再复发。

---

## [1.0.13] — 2026-09-17

> ✅ **已实测通过**（用户实测："效果理想"）—— 第一人称的刀与手同步摇晃，
> 且跟随游戏内置的「视角摇晃」开关。

### 新增

- **第一人称的刀会跟着脚步摇晃，与手部同步。**

  原版第一人称的手（以及手里的物品）会随脚步摇晃，而拔刀剑的第一人称渲染会把姿态栈清零，
  摇晃正刻在被抹掉的那份矩阵里 —— 于是**手在晃、刀不晃**，两者不协调。

  - **直接调用原版的 `GameRenderer#bobView`**（经一个 `@Invoker` 混入），**不重写那几行数学**：
    与手部**逐像素一致**；Minecraft 日后改动摇晃算法时本模组会**自动跟随**，不会悄悄漂掉。
  - **绑定游戏内置的「视角摇晃」开关**：玩家关掉它，刀也不晃。
  - 插入位置在姿态基准**之后**（`ModelViewMat⁻¹ × 相机旋转 × 摇晃`），效果是**整把刀随视角摇**，
    而不是绕刀自身原点自转；有/无光影结果一致（与 1.0.12 的姿态基准同源）。
  - 开关 `firstPersonBladeBob`（默认 `true`，**热重载**）。

  > ⚠️ **这是本模组有意添加的增强，不是"还原 1.20.1"。**
  > 1.20.1 的第一人称刀**是不晃的**（它同样清零了姿态栈）。
  > 不需要这个效果就把 `firstPersonBladeBob` 设为 `false`。

- **独立混入配置 `yessb.bob.mixins.json`**（`required=false` + `defaultRequire=0`）。
  `bobView` 是**私有**方法，名字与签名跨版本可能变，因此本项与其它混入**隔离**；
  借不到时只有这一项失效（调用方会捕获并永久停用），**其余功能与游戏不受影响**。
  这是 1.0.9 那次「一条注入失败连累整份混入」之后定下的规矩。

### 修复

- **配置模板与实际默认值不一致（`firstPersonPoseBase`）。**
  该字段的默认值在 1.0.12 已改为 `camera-view`，但**配置模板里仍写着 `modelview`** ——
  那是一条**已实测会在无光影下把刀摆错**的模式，于是"新生成的配置文件"会拿到错误的值。
  （已有配置文件不受影响：那里的值优先。）
  现已修好，并做了一次**全量审计**：**53 个配置字段与模板逐项一致**。

### 诊断

- 新增一行状态日志（**只在翻转时打**）：
  `[YES-SB] 第一人称刀的视角摇晃：开/关（跟随游戏内置「视角摇晃」设置）`
  —— 与 1.0.12 那条"混入是否生效"的自检行同一思路：回答"这个功能到底有没有在跑"。
- 就绪日志补上 `第一人称基准=…` 与 `第一人称视角摇晃=…`。

---

## [1.0.12] — 2026-09-17

> ✅ **已实测通过。** 修好了**开光影后第一人称的刀跑到别处、并随移动漂移**的问题。
> 无光影下的表现与修复前**逐位相同**（见下方"为什么不会影响无光影"）。
>
> 1.0.9 / 1.0.10 / 1.0.11 是排查期的内部测试构建，**从未发布**
> （1.0.9 的混入因靶点校验失败整份作废；1.0.10 的基准推导被实测否证），不单列。

### 修复

- **第三人称腰侧的挂刀整体偏低（挂在大腿外侧）**

  **现象**：加装后腰侧的刀整体偏下。**所有 YSM 模型包都偏低，内置默认模型也一样。**

  **根因**：不是模型包的问题。腰挂刀的补偿变换是照**原版玩家模型**的比例复刻的，
  而 YSM 模型渲染出来比原版玩家模型矮，于是按原版比例算出来的腰挂点整体**下沉**。
  因为是**固定下沉**（所有模型包一致），一个常数即可收敛，不需要按模型包配表。

  **修法**：把实测标定值固化进默认值 —— `waistOffsetY` 默认 **`0.2`**（方块，正数 = 向上）。

  > **一个容易踩的点（已记入 §7 坑清单）**：这个 `0.2` 早前只是在排查用的那个实例里
  > 手工调出来的，**只存在于那份实例的 `config/yes_sb.properties`**，从未写进代码默认值。
  > 而配置文件是**按实例存放**的（`<实例目录>/config/yes_sb.properties`），
  > 把 mod jar 复制到别的实例**不会带上配置** ⇒ 新实例拿到的是出厂默认 `0`，
  > 看起来就成了"装了之后刀偏低"。

- **开光影后第一人称的拔刀剑位置错误、且随移动漂移（Iris / 任意光影包）**

  **现象**：只要启用任意光影（不限种类），第一人称的刀就摆到别处，并且走动时会在画面上漂移；
  关闭光影则一切正常。这个问题**与光影包的类别无关**，也不涉及 BSL／Advanced Materials。

  **根因**（逐条由字节码确证）：

  1. 一个顶点最终的变换是 `ModelViewMat × 姿态栈`；
  2. 无光影时 `GameRenderer#renderItemInHand` 令
     **姿态栈 = 相机旋转的逆**、**`ModelViewMat` = 相机旋转**（两者互补）；
  3. 拔刀剑的第一人称渲染会把姿态栈**清零**（`BladeFirstPersonRender` 里的
     `me.pose().identity()`）—— 清零之后最终变成 `相机旋转 × 刀变换`，
     **刀"跟着视角"正是靠这一层相机旋转**；
  4. 而 Iris 会把 `ModelViewMat` 换成它自己那个「视角摇晃矩阵」
     （`iris.pathways.HandRenderer`，**不含相机旋转**），姿态栈也从单位矩阵开始
     ⇒ 清零之后**少了一层相机旋转、多了一层摇晃** ⇒ 位置不对 + 随移动漂移。

  **修法**：把那次"清零"替换为基准 `ModelViewMat⁻¹ × 相机旋转`。

  - 无光影：两者相等 ⇒ 该式 = 单位矩阵 ⇒ **与修复前逐位相同**；
  - 开光影：正好把 Iris 多塞进去的那层抵消掉 ⇒ 与无光影表现一致。

  代码里另有一道**结构性保险**：先比较 `ModelViewMat` 与相机旋转，
  **相等就直接按原样清零** —— 所以"无光影零改动"不依赖等式在数值上恰好成立。

  > 附带说明：修复前那个"开光影时刀会晃"的观感，其实**是 bug 的表现**——
  > 那是手部的视角摇晃矩阵漏到了刀上。修复后它不再出现，与无光影、与 1.20.1 一致。

- **第一人称姿态基准修正在 1.0.9 整份失效**（内部构建，一并记录）

  1.0.9 曾把"停用拔刀剑自带的 Iris 近似补偿"也做进同一个混入类，但那条注入在
  Mixin 的**靶点校验阶段**就失败，而 `require = 0` **挡不住这种失败** ——
  它让**整份混入类**作废，于是连姿态基准修正也一起没注入（表现为"与旧版完全一致"）。
  现已拆开：**一个混入类只放能过校验的靶点**，并且每个混入类都带一条
  **"我还活着"的自检日志**，实测时一眼就能看出跑的到底是不是修好的那份。

### 新增

- `firstPersonPoseBase` 配置项（**热重载**，默认 `camera-view`）：
  - `camera-view` —— 默认，上述修正；
  - `identity` —— 拔刀剑原行为（无光影正确，开光影时有问题），排错时可用；
  - `modelview` —— 仅供对照，正常不要用。
- 诊断行（`debugLog=true`，每 2 秒一条）：
  `第一人称[基准]：采用=… ModelView=… 像相机旋转=是/★否 入口姿态=…`
- 自检行：`第一人称姿态基准修正已注入（MixinBladeFirstPersonRender 生效）`

### 说明

- 本修正**不检测光影包、不判断光影种类**，对任意光影同样成立；
  未安装 Iris 时它完全惰性（`ModelViewMat` 与相机旋转相等 ⇒ 恒等替换）。

---

## [1.0.8] — 2026-09-15

> **功能代码零改动。** 本版只做收尾：把已验证完毕的诊断噪音剪掉，并明确 `debugLog` 的用途。

### 变更

- **`debugLog` 保留**（它是本模组唯一可被外部验证的接口 ——
  本模组做的多是"补一层被掐掉的渲染、补一个恒为假的返回值"这类
  **看不见就等于没干活**的事，关掉它用户连"功能有没有生效"都说不清）。
  但把配置说明改写清楚了：**报 bug 时开一次，把所有 `[YES-SB]` 开头的行附上**，
  并列出开启后能看到哪几类信息。

- **剪掉三处纯噪音**（都是"当时为了证明某条路活着而加、现在已经验证完毕"的）：
  - `女仆刀：主手 已由 TLM 路径画出（…）` —— 有女仆在屏幕上时每 2 秒刷一条
  - `女仆背槽刀：已由 TLM 路径画出（…）` —— 同上
  - `女仆刀：出鞘状态=否（…）` —— 每次攻击翻转两次就是两条，而"收回鞘里"没有信息量

    保留的是**失败路径**（如"没有定位组骨骼 ⇒ 不接管"）与**调参信息**
    （第一人称视野判定、腰挂层位移）。

- **"出鞘状态"刻意保留了每个实体的第一条**（通常是"否"，文案标为"基线"）。
  万一"客户端时间戳链"整个断了、"是"永远不出现，日志里至少还有这条基线，
  **不会安静得像成功一样** —— 这是上一轮刚踩过的坑（诊断把关键信息藏起来），不再重犯。

### 说明

- 女仆**播不播剑技动画**不在本模组范围内：无其他模组时，女仆的拔刀剑攻击
  默认就是普通挥动，这是参照版本本来的行为。本模组只负责补回"刀光 + 出鞘"，
  补完之后女仆的表现即与参照版本一致。

---

## [1.0.7] — 2026-09-15

> ✅ **已实测通过。** 女仆拿拔刀剑攻击时**刀光会出现、刀会从鞘里出来**；
> 服务端 / 客户端两侧的信号都由日志逐条证实（两侧各 15 条一一配对、时延 4–11 ms、
> 出鞘窗口 `elapsed` 0 → 5 刻精确吻合、两侧累计次数一致 ⇒ 没有重复执行）。

> ⛔ **先读这条**：**1.0.6（构建于 14:39 的那一份）无法加载，已经作废。**
> 它把游戏直接打进了"broken mod state"，而表现出来却是一条来自 Sodium 的、看起来毫不相关的崩溃。
> 原因与处理见下。**1.0.7 = 1.0.6 的全部内容 + 去掉那个致命字节 + 加一道构建期守卫。**

### 修复（严重）

- **`neoforge.mods.toml` 被写入了 UTF-8 BOM，导致整个 jar 不是合法模组。**

  崩溃现场（`错误报告-2026-9-15_14.39.13\latest.log`）：

  ```
  [Render thread/FATAL] [net.neoforged.fml.ModLoader/CORE]:
    Error during pre-loading phase: File mods\YES_SB-1.0.6.jar is not a valid mod file
  com.electronwill.nightconfig.core.io.ParsingException: Invalid bare key: '﻿modLoader'
  ```

  注意那个 `'﻿modLoader'` —— 前面有一个看不见的 BOM。
  TOML 的第一个键于是从 `modLoader` 变成了 `\uFEFFmodLoader`，加载器解析失败、拒绝整个文件；
  NeoForge 随即进入 "broken mod state"（日志里刷了一屏
  `Cowardly refusing to send event … to a broken mod state`），
  最后由 Sodium 抛出那句极具误导性的
  `Sodium's config could not be found; the game is in a broken state most likely caused by an earlier error from another mod`。

  **根因是一行 PowerShell**：本机是 **Windows PowerShell 5.1**，它的
  `Set-Content -Encoding UTF8` **会写入 BOM**（PowerShell 6+ 才不会）。
  改版本号时用它重写了这个文件，于是 BOM 进了 jar。
  ⇒ 已**逐字节**去掉 BOM（其余内容一个字节都没动）。

- **加了一道构建期守卫（`build.ps1`）**：打包前扫描 `src` 与 `src\main\resources`，
  任何文件只要以 `EF BB BF` 开头就**直接让构建失败**并列出文件名。
  这类错误只会在真机上以"别的模组崩了"的形式出现，必须挡在构建阶段。
  （守卫已实测：植入一个带 BOM 的文件后，构建按预期失败。）

### 沿袭 1.0.6 的全部内容（功能代码零改动）

1.0.5 的女仆修复**本身是好的**（已实测：刀光出现、刀会出鞘）。1.0.6 只改诊断：

- **修掉一个会把关键信息藏起来的节流缺陷**：原来"女仆挥砍"只有一个共享的 2 秒节流，
  而服务端那次 `swing()` 与客户端那次（动画包回来）**只差约 1 刻** ⇒ 客户端那条**必定**被吃掉
  （实测日志里 6 条服务端、0 条客户端，而"客户端跑没跑"恰恰最需要看见）。
  现改为**按侧各自计数与节流**：每侧前 3 条必打，"结果变化"也一定打。
- **早退带原因 + 异常留痕**：`让路（上游已自己声明 swing 覆写）` /
  `跳过（该手不是拔刀剑）` / `跳过（任务不是攻击任务）`（含读到的任务名，读不到显示
  `?(读取失败)`）/ `★异常 …`。不再让"没生效"和"判定不通过"长得一样。
- **新增"出鞘状态"诊断**（翻转才打一条）—— 这是"客户端时间戳真的生效"的唯一观测点。
- **修掉每次启动假报一次「配置已热重载」**：`ensureLoaded()` 现在会记下文件时间戳。

### 说明（不是问题）

- 升级后首次进入已有存档时，NeoForge 会打印
  `The following mods have version differences that were not resolved: yes_sb (…)`。
  这是它拿存档里记录的模组版本与当前版本做对比的**常规提示**；本模组不在存档里存任何数据，可以忽略。
  **发布更新说明时值得提一句，免得用户以为是坏了。**

---

## [1.0.6] — 作废

> ⛔ **从未成功运行过。** 该版本因 `neoforge.mods.toml` 带 UTF-8 BOM 而无法加载，
> 游戏在启动阶段即崩溃。内容已全部并入 **[1.0.7]**，请勿使用本版。

---

## [1.0.5] — 2026-09-16

### 修复

- **车万女仆拿着拔刀剑攻击时没有刀光，刀也不会出鞘。**
  这是 TLM 1.21.1 发布版丢掉的**第三处**（前两处是手部与背槽渲染）。
  TLM 的 `1.20`/`1.21` 分支里 `EntityMaid` 覆写了 `swing()` 去调拔刀剑的挥刀兼容，
  而发布版里**连这个覆写都不存在**（`javap -p` 全表只剩 `isSwingingArms`/`setSwingingArms`，
  全 jar 对 `slashblade` 的引用数为 0）。于是女仆攻击正常、伤害正常，但既没有斩击特效、
  刀也一直插在鞘里不动。

  补回后有两个副作用，分别属于两侧 —— 这也是上游把钩子挂在 `swing()` 上的原因：

  | 副作用 | 生效的一侧 | 机制 |
  |---|---|---|
  | 刀光（斩击特效） | **服务端** | 生成特效实体，由原版实体同步自动发给所有客户端 |
  | 刀模出鞘动作 | **客户端** | 写客户端自己那份物品上的时间戳，渲染层读的就是它 |

  > 顺带纠正一个容易想当然的地方：拔刀剑的 `AttackManager.doSlash(...)`
  > **在客户端第一句就 `return null`**（1.9.65 与 2.0.7 逐条一致），
  > 所以"客户端调它来生成本地刀光"是行不通的，刀光只能由服务端出。

- 新增开关 `maidSlashBladeAttack`（默认 `true`）。
  与已有的渲染开关 `maidSlashBlade` **分开**，便于对照排查——
  关掉它、只留渲染，可以单独确认"刀在腰上但不挥"。

### 说明

- 本项**不注入车万女仆**：因为发布版里那个覆写方法已经不存在，
  改为挂在**原版** `LivingEntity#swing(InteractionHand)` 上自行判定"是不是女仆"。
  也就是说这一处没有修改车万女仆的任何字节码。
- 带**让路闸**：若上游日后自己把 `EntityMaid#swing` 加回来，本项会自动失效，
  不会出现同一帧两遍、两道刀光的重复现象。
- 归属与协议见 `NOTICE` 第二节（复刻的是其**行为语义与数值**，代码为自行编写）。

---

## [1.0.4] — 2026-09-15

> 1.0.2 / 1.0.3 是排查期间内部使用的测试构建，**从未发布**，此处不单列。
> 相对 1.0.1 的全部变更如下。

### 修复

- **切换物品时，第一人称会冒出一把横向的刀并跟着装备动画下落。**
  根因在 `auto` 模式的兜底分支：它的前置条件里最后一句是"主手是不是拔刀剑"，
  而切换物品时手上已经是新物品，于是被误判成"原版画不出来 ⇒ 本模组兜底"，
  把**正在离场的那把刀**按手持物画了出来。
  现在只有在**玩家此刻确实握着拔刀剑**时才接管第一人称。
  判据刻意用 `mc.player` 的双手，而不是传进来的那个 stack ——
  装备动画里那个 stack 正是离场的刀，拿它判断等于没判断。

### 诊断（`debugLog=true`）

排查期间补的几条，全部保留：

- `[YES-SB] 剑技动画为空 ⇒ <原因与实测数值>` —— 这一支**原先完全静默**，
  日志里一条都没有时分不清是"没被调用"、"超时"还是"异常被吞"。
  现在会打出 `seq` / `elapsed` / `timeoutMs` / `lastActionTime` / `gameTime` / 帧区间，
  并且**按原因去重**。
- `[YES-SB] 剑技动画：最近 2 秒 YSM 询问了 N 次` —— 用于区分"YSM 没走到这条分支"
  与"走到了但算出来是空"。
- `catch (Throwable)` 分支原先**完全不留痕**，现在会报告异常类型与消息。

### 已知问题（未解决，且已确认不是本模组造成的）

- **空中连招（右键连按）第二段收招后，模型停在滞空姿势，落地也不切回地面动画**，
  需要按一次鼠标左/右键才能恢复。移动、疾跑、跳跃都无法解除。

  排除过程（全部由实测得到）：

  | 测试 | 结果 |
  |---|---|
  | 两条动画注入**全部关闭** | 不复现 —— 但那样也没有任何拔刀剑专属动画 |
  | 只开 `slashbladeComboAnimations` | 复现 |
  | 只开 `slashbladeMainStateAnimations` | 复现 |
  | `enabled=false`（整个模组惰性） | 不复现（同样是没有动画可播） |

  ⇒ **只要让 YSM 去播模型包的 `slashblade:*` 动画，这条就可能出现**；
  与连招名怎么算、主动画变体怎么替换都无关。

  **归因：这是 YSM 本身的行为，与 Minecraft 版本无关 ——
  在 1.20.1（YSM 2.6.5-forge ＋ 同一套模型包 ＋ 拔刀剑 1.9.65）上现象完全一致。**
  详见 README §6.6 与 `上游反馈稿-YSM-空中动画.md`。

---

## [1.0.1] — 2026-09-14

### 修复

- **第一人称看不到刀（已定案）**：根因是 `renderBladeModel` 漏了一次
  `translate(0.5, 0.5, 0.5)` —— `ItemRenderer#render` 会无条件做 `translate(-0.5,-0.5,-0.5)`，
  而拔刀剑自己的 `renderBlade` 进非第一人称分支后第一句就是把它抵消回去。
  本模组此前只给 `FIXED` 分支补了这一句，`hand()` / `firstPerson()` 都没有，
  于是这两个上下文整体偏了 0.5 个世界单位，再被 display 的 `scale 1.25` 与 `rotY(180)`
  放大成约 0.6 格 —— 第一人称下刚好把刀推到画面下方之外。
- **`*Offset*` 参数调不动**：位移原本写在 `poseStack.scale(...)` **之后**，
  于是被缩放系数乘了两遍（填 `1.0` 实际只挪 `0.0062` 格）。现已移到最外层，
  配置值就是真正的世界位移（方块）。

### 变更

- **第一人称默认不再由本模组接管**（`firstPersonMode` 默认由 `model` 改为 **`auto`**）。
  第一人称**不经过** YSM 接管的 `EntityRenderDispatcher`，YSM 从来没有掐过它；
  1.20.1 的样子就是拔刀剑自己的 `BladeFirstPersonRender`（"一整把刀斜跨画面"），
  1.21.1 上它本来就是好的。`auto` 会先按 `BladeFirstPersonRender#render` 的字节码条件
  逐条询问原版"这一帧你画不画得出来"，画得出来就放手，画不出来才兜底。
  `off` / `model` / `icon` 保留为可选。

### 诊断

- `debugLog=true` 时新增两条日志：
  - `手持刀[firstPerson]：… 刀原点(相机空间)=(x,y,z) 视野判定=在画面内/★在画面外`
    —— 直接按当前 FOV 与窗口宽高比算出"刀在不在画面里"，调 `firstPersonOffset*` 不用再猜；
  - `第一人称[原版]前置条件：…⇒ 原版会画/★原版画不出来` —— 原版遇到任何一条不满足都是**静默 return**，
    这条日志把它摆到台面上。

### 实测

- 第一人称的刀正常（就是参照图里那"一整把刀斜跨画面"的形态）。**通过 ✅**

---

## [1.0.0] — 2026-09-13

首个公开版本。修复四块：

1. **第三人称腰挂刀补偿渲染** —— YSM 在 `EntityRenderDispatcher#render` 里跳过被接管实体的
   原版渲染器调用，连带掐掉了挂在渲染器上的拔刀剑 `LayerMainBlade`，导致腰挂刀 / 刀鞘完全不渲染。
   本模组用"打卡"判定（原版渲染器跑没跑）决定是否复刻原版变换环境并调用 `LayerMainBlade`；
   原版跑过就一个像素都不画，避免重影。YSM 的**纸娃娃**走同一个钩子，因此一并修好。
2. **剑技动画触发** —— YSM 2.6.5 的拔刀剑联动模块是空实现（`isSlashBlade` 恒 `false`、
   取动画名恒 `""`），使它自己的解析器里那段取动画名的逻辑成为死代码。
   本模组只补回**两个返回值**，规则逐条对齐上游公开源码（含 `combo_a4_ex → combo_a4ex` 归一化、
   `standby` 超时 `-553`、居合斩空中变体）。算出的动画若不在模型包里则返回**空串**，
   避免 YSM 解析器 `return CONTINUE` 压住普通挥动逻辑。
3. **主动画的拔刀剑变体** —— 持刀时 `slashblade:idle/walk/run/...`，模型包没有则退回原名。
4. **车万女仆的拔刀剑** —— TLM 1.21.1 发布版丢了 `compat/slashblade` 整包，
   女仆手里的刀掉进第三人称物品上下文（拔刀剑在那里什么都不画），背槽掉进 `FIXED`
   被画成平面图标。本模组按 TLM 源码**原样补回**那两处分支，变换数值一个没改。

另含：

- 第一人称 / 手持 / `FIXED` 三个上下文**各自独立**的缩放与变换参数（热重载）；
- `FIXED` 上下文的**几何自动修正**（平面图标与 3D 刀身的包围盒中心差约 117 模型单位）；
- 四处靶向注入的失败保护：`required=false` + `defaultRequire=0` + YSM 版本白名单（默认 `2.6.`），
  任何一条不满足即整体不注入 —— 表现为功能不生效，**绝不崩溃**。

---

## 已知问题（不随版本修复）

- **玩家的刀跟不上 YSM 的动画**：刀挂点由**原版**玩家模型算出，屏幕上却是 **YSM 模型在播 YSM 动画**，
  两套动画永远对不上。彻底对齐需要绑定 YSM 模型自身的定位组骨骼，**暂不做**（理由见 README §6.1）。
- **女仆的剑技动画不在本模组范围内**：本模组补回的是**刀光 + 出鞘**（1.0.5 起），
  而**无其他模组时女仆的拔刀剑攻击默认就是普通挥动** —— 这是 1.20.1 参照版本**本来就有**的行为，
  **不是缺陷，故不需要处理**。想让女仆真的打出连招，请交由
  [车万女仆：真正的力量（TLM: True POWER）](https://modrinth.com/mod/true-power-of-maid)；
  本模组只保证其**触发钩子**可用。
- **上游修复后本模组即退役**：YSM 作者已答复"后面会做"这份适配。详见 README §七。
